To run the site locally paste the following command into Terminal:
bundle exec jekyll serve
then go to localhost:4000 in the browser

common git commands to use in the terminal:
git status
git push
git pull